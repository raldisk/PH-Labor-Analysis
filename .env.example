# ph-labor-analysis — environment configuration
# Copy to .env and set your PostgreSQL DSN.
# Without this, the notebook uses bundled CSV data automatically.

# PostgreSQL DSN from ph-economic-tracker
# Leave unset or blank to use CSV fallback data
PH_TRACKER_POSTGRES_DSN=postgresql://tracker:tracker@localhost:5432/ph_economic
